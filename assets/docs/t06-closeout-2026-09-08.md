# T06 closeout

Date: 2026-09-08

Scope: bounded regional locale draft generator protections for `scripts/build-locale-drafts.py` and the dedicated locale-draft regression coverage.

## What changed

- Added `tests/test-locale-drafts.py` to pin the generator contract for:
  - reviewed es-MX draft input preservation
  - noindex draft shell behavior
  - MurderBird copy-boundary handling in visible text adapters
  - canonical source hash normalization
- Added `tests/test-locale-drafts.py` to the validation workflow so CI exercises the new contract explicitly.

## Verification

- `python3 -m unittest tests/test-locale-drafts.py -v`
- `python3 -m unittest tests/test-i18n-release.py tests/test-i18n-workflow.py tests/test-homepage-hero-parity.py -v`
- `python3 scripts/check-regional-drafts.py`

## Observed state

- The reviewed es-MX draft inputs remain untouched by the generator run used in the test suite.
- The draft pages still render as `noindex` and keep public alternates out of the output.
- The homepage hero still resolves to the current PNG fallback path in the rendered drafts.

## Remaining limitations

- This package does not add a new runtime check that rejects every possible public Spanish source variant. The current protection is the reviewed-input path discipline enforced by the generator call site plus the regression coverage above.
- No translation content was reauthored in this package.
