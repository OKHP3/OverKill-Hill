# A08 supported QA runtime

September 7, 2026. Baseline: `98922aebf71d90b2b18ecc34c8b00a041fff51c7`.

## Finding and change

D03 reproduced: both active CI Node selectors use 20, and no local package
engine contract exists. The [official release table](https://nodejs.org/en/about/previous-releases)
lists Node 20 EOL and Node 24 LTS. The [official schedule](https://github.com/nodejs/Release/blob/main/schedule.json)
places Node 24 in Active LTS until October 20, 2026, with support ending
April 30, 2028. Both sources were checked September 7, 2026.

`.nvmrc` now selects 24 for version managers and both CI jobs. Package engines
and root lockfile metadata require `>=24 <25`; `.npmrc` enables strict engine
checking during installation. README documents setup and the upgrade policy.
The major selector permits current patches without pinning CI to an aging
patch. Dependency names, versions, resolved URLs and integrity values are
unchanged, confirmed by comparing parsed lockfiles after removing only the
new root engine metadata.

A07 confirmed ownership of artifact/freshness changes; A08 touches only the
two Node selector lines in the workflow. A10 and A17 received this contract
and confirmed their independent QA/concurrency scope.

## Local validation

Windows, Node v24.11.1, npm 11.6.2, existing Playwright 1.62.1 and Chromium.
This tests the selected major on the available patch; it is not evidence of
an Ubuntu CI run or testing the newest 24.x patch. Browser suites used the
isolated checkout at `http://127.0.0.1:5088`; the universe fixture starts its
own server. No other worktree or server was changed.

| Check | Result |
| --- | --- |
| `npm install --package-lock-only --ignore-scripts` | PASS; only root engines added |
| `npm ci` | PASS; clean installation, 2 packages added, 0 audit vulnerabilities |
| `npx playwright install chromium` | PASS |
| `node --test tests/search-page.test.mjs tests/search-index.test.mjs tests/search-index-locale.test.mjs tests/embed-workflow.test.mjs tests/mermaid-links.test.mjs tests/csp-qa.test.mjs` | PASS; 20 tests, no skips |
| `npm run test:accessibility -- --base-url=http://127.0.0.1:5088` | PASS; 4 representative pages and 31 public routes |
| `npm run test:csp -- --base-url=http://127.0.0.1:5088` | PASS; 31 routes, 21 diagrams, 0 route failures; expected blocked external origins reported |
| `npm run test:phone-overflow -- --base-url=http://127.0.0.1:5088` | PASS; 31 routes at 320px |
| `npm run test:toc -- --base-url=http://127.0.0.1:5088` | PASS; route follow checks and legacy MediaQueryList transition |
| `node tests/test-universe-browser.mjs` | PASS; 5 diagrams, links, two widths, theme switch and no-JavaScript fallback |
| `node scripts/responsive-qa.mjs --base=http://127.0.0.1:5088` | PASS; actual Playwright mode, 310 checks, 0 failures |
| `py -3 scripts/validate-site.py` | PASS; 0 errors, 32 existing warnings; no new voice warnings |
| `py -3 scripts/build-site.py --check` | PASS; 36 generated pages |
| `py -3 scripts/build-search-index.py --check` | PASS; 160 index entries |
| Runtime selector/engine/lockfile agreement and `npm config get engine-strict` | PASS |
| `git diff --check` | PASS |

The responsive runner rewrote its tracked machine report. Its result was
copied to ignored `test-results/a08/responsive-results.json`, then the tracked
report was restored byte-for-byte from the baseline. Local `.tmp-a08-*.log`
files retain console output and are ignored. No generated site output changed.

## Remaining evidence

- CI on Ubuntu and the newest Node 24 patch: NOT RUN. A21 must run the
  combined workflow and retain the actual resolved runtime versions.
- Replit runtime: NOT VERIFIED. `.replit` still declares `nodejs-20`.
  Its preview, contrast and locale workflows execute Python 3.11 commands,
  and deployment is static; production has no Node server. Node QA in that
  environment is not accepted. A connected Replit worker must verify module
  availability and Node 24 execution before changing its platform selector.
- Broader shipped-route coverage and mandatory browser failure reporting
  remain A10 scope. These results state each existing suite's actual coverage.
- No merge, deployment, account change or dependency upgrade was performed.

A08 is ready for integration of the local/CI contract with these explicit
execution limits. This is not a claim that the Replit part of D03 is resolved.
