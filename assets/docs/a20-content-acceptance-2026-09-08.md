# A20 independent content-wave acceptance

**ACCEPT WITH LIMITS for `ed9a47d0e7e247977de8bd58ba10ac9376fa1ed5`.** The reviewed content/status corrections, bounded locale updates, image derivative, installation preservation, and targeted visibility/search behavior pass the checks below. The historical fallback-only test remains FAIL; its continuing image and narrative requirements were independently tested and pass. This is scoped local acceptance, not hosted/live acceptance, native-language approval, accessibility certification, or publication authorization.

## Candidate and package

Baseline: `c2d23f088d5c29cc3df5c2e8f1b5eb9d479da61a`. A20 reviewed the exact candidate on `codex/a20-content-acceptance` in `C:/Users/jamie/.codex/worktrees/269e/overkill-hill`. The candidate and supplied package remained unchanged; this evidence commit adds only this report and three JSON files.

Package supplied by A21: `C:/Users/jamie/.codex/worktrees/ace6/overkill-hill/.local/a21-evidence/content-release`, copied to A20's ignored `.local/a20-content/release`.

Manifest `assets/audit/release-manifest.json` SHA-256: `2ac0a0e44941c2fa5aef10f0b5fabb806868ff4fbce489f8d1337d2a11476687`. The repository verifier passes **56 HTML pages, 372 files**. An independently authored verifier checks that hash, candidate identity, exact inventory, and every one of the **371 payload lengths/digests**. All payloads match candidate Git blobs: 211 exactly, 160 with only CRLF/LF differences, zero other mismatches. This is Windows package evidence, not proof of identical future Linux-hosted package bytes or an independent signature. See [integrity and preservation evidence](../audit/a20-content-integrity-preservation-2026-09-08.json).

## Claims, status, and source consistency

| Reviewed change | Independent assessment |
| --- | --- |
| Workbench journal and readiness | The page separates May history from current operation, names the six-model/five-test May 30 sample and its formatting defects, and retains unverified RAG ingestion/retrieval/citation. A20 retrieved the pinned benchmark and August readiness records; both support these qualifications. The retained historical hero wording and earlier plan are bounded by the visible journal explanation. No current workstation operation was tested. |
| Three concept pages | Hometools, PathScrib-R, and Un-NOCKed Truth have explicit planned-capability notices, return/contact links, and noindex. They are excluded from the public search index and main shelf by the current registry/checker. Independent no-JS phone-width samples confirm the notices are visible in Chromium and Firefox. |
| First-try and universal-success statements | Reviewed source diffs change BPMN/MTB guarantees to design intent and required output checking, Prompt Forge self-report to evidence requiring comparison, BFS influence to a hypothesis, and homepage blanket road-testing to a goal. They do not establish new measured success rates. |
| FoundRy license summary | The new text distinguishes optional promotional credit from license/notice obligations, and separates exported/third-party rights. A20 checked Apache section 4; the narrower summary agrees with those requirements. This is not a ruling on rights in a particular export. |
| Featured labels and Vault/workbench previews | Homepage/writings sections stop asserting chronological recency; Vault identifies the one downloadable template and planned resources. Workbench metadata, shelf description, and JSON-LD reflect a dated journal and unverified retrieval. No invented publication date is needed. |
| Canonical project records | All 14 detail routes plus four explicit shelf exceptions are accounted for in the 18-record registry. Records distinguish availability, maturity, source-described evidence, review date, and unknown delivery. The source schema refuses to convert source review into runtime proof. Generated detail/shelf/search values agree, and 32 independently run route/viewport checks confirm public HTML identity, rendered values, visible summaries, no overflow, and private registry denial. Removing duplicated status pills does not remove the archive. |

Sources independently retrieved September 8, 2026: [pinned benchmark record](https://raw.githubusercontent.com/OKHP3/mac-studio-local-ai-workbench/863d48a80958d91452e5ccac0bb0d181f7192688/docs/05-benchmark-results.md), [pinned August readiness report](https://raw.githubusercontent.com/OKHP3/mac-studio-local-ai-workbench/863d48a80958d91452e5ccac0bb0d181f7192688/reports/openclaw-related-runtime-readiness-audit-2026-08-02.md), and [Apache License 2.0](https://www.apache.org/licenses/LICENSE-2.0). These substantiate the changed qualifications, not all unrelated historic claims in the portfolio.

## Historical test adjudication

The complete `tests/test-murderbird-fallback-editorial.py` contract asserts both whole-page extracted text and ordered image alt/title equality against `d112ca4eb091c0567fc3fd4f3f307168dd38a552` for English and French homepages. It stops on its first assertion failure; emitted hashes are not additional checks of image bytes or original article text.

The unchanged test was executed and failed at English whole-page text equality. A20 independently evaluated both pages, confirming **two whole-page text mismatches** and **two passing image alt/title comparisons**. The text mismatches reflect authorized qualifications, status summaries, Featured wording, and French changes. A content wave cannot satisfy a fallback-only promise of unchanged whole-page text. That premise is superseded for this explicitly authorized change, not declared passing or dismissed merely because the test is absent from CI.

Continuing protections independently PASS: the original ETCH PNG is byte-identical to the content baseline; the new lossless WebP decodes to identical RGBA dimensions/bytes; four light/dark phone/desktop image comparisons have identical geometry and at most 1/255 browser-scaling channel difference. All 11 protected main-text paths remain identical after explicit UTF-8 and CRLF normalization: eight narrative/field-guide HTML pages under writings, plus manifesto, about, and contact. The intentionally relabeled writings hub is explicitly outside that unchanged-text set. The original historical test file remains unchanged.

This adjudication accepts the authorized content delta while retaining the two historical failures in machine evidence. It does not authorize future unreviewed prose/art changes. A16's original performance trial totals were not independently rerun here; acceptance covers the derivative's integrity and visual parity, not a new speed or conversion claim.

## Locale and visitor evidence

Actual French freshness passes with eight existing de/es advisories. Regional validation passes all eight en-gb/es-mx pages with noindex, no public alternates, and zero public search entries. The French provenance explicitly retains AI review, human/native approval false, and existing condensed-page omissions; a green hash is not full English/French content parity. The regional records retain machine-drafted status, provisional inherited voice, human/native false, and release acceptance false. They do not promote regional drafts to publication.

The reviewer inspected changed en-US/es-MX units and their retained evidence URLs/versions/uncertainty, plus the French scope record. This is bounded source/meaning review and browser acceptance, not native-language validation or independent reapproval of voice profiles. A14 alternatives and optional status disclosure are absent from this candidate; no presentation choice was inferred.

Fresh five-task runs used Windows, Node v24.11.1, Playwright 1.62.1; Chromium 151.0.7922.34, Firefox 153.0, and WebKit 26.5. The exact package was served on loopback HTTPS port 5221 with a temporary self-signed certificate and `ignoreHTTPSErrors: true`; no trust-store, CSP, or candidate modification occurred. External requests were blocked, so font/Analytics/embed availability is not covered.

| Task | Chromium / Firefox | WebKit |
| --- | --- | --- |
| Read homepage in normal, no-JS, blocked-app, missing/throwing/stalled observer, reduced-motion states | PASS all eight modes, heading painted and art loaded | PASS all eight modes |
| Skip navigation and continue within main | PASS | FAIL retained; ordinary-link Tab traversal has the previously isolated environment limitation |
| Compact keyboard navigation to Contact at 390 x 844 | PASS | FAIL retained under the same link-traversal limitation |
| Search `mermaid`, inspect focused ordinary result and clean excerpts, exercise bounds/Escape; dedicated `MurderBird` Enter | PASS | PASS |
| Activate story reading shortcut and continue within main | PASS | FAIL retained under the same link-traversal limitation |

The prior plain anchor/button fixture isolated WebKit's ordinary-link Tab behavior from the site. It is not reclassified as a successful WebKit task or actual Safari result. See [fresh task evidence](../audit/a20-content-tasks-2026-09-08.json).

Twelve additional Chromium/Firefox samples PASS: three concept notices with JS disabled, French focused search/theme/fallback labels, and both regional project shelves with noindex and no horizontal overflow at 390 x 844. See [content and locale browser evidence](../audit/a20-content-details-2026-09-08.json).

## Independently executed checks and limits

PASS: `scripts/build-release.py --verify` with the exact candidate/package; `scripts/build-site.py --check` (36 pages); `scripts/build-search-index.py --check` (160 entries); `tests/test-project-status.py` (7); `scripts/check-project-status.py` (18 records); `scripts/check-i18n-release.py --mode check --format text`; `scripts/check-regional-drafts.py`; `node --test tests/search-page.test.mjs tests/french-interactions.test.mjs` (17); `node tests/test-reveal-browser.mjs` (10); `node tests/test-project-status-browser.mjs --base-url=http://127.0.0.1:5220` (32); `node scripts/check-etch-parity.mjs --base=http://127.0.0.1:5220` (4); and `tests/test-skillz-install-guide.py --archive .local/a20-wave-one/skillz.zip -v` (5). Python commands used the installed Python launcher/interpreter; no dependencies were added. `git diff --check` and evidence JSON parsing pass.

The exact installation recipe text is unchanged from the content baseline. Its five tests recheck public Git-tree byte integrity, complete installation into both client paths, supporting generator tests, and refusal to overwrite. Client activation remains untested. Browser tests on HTTP port 5220 used the unchanged candidate's allowlisted preview; the independent five-task tests used the copied package over HTTPS. Disposable scripts/logs/screenshots are under ignored `.local/a20-content`, `output/a20-content-*`, and the parity runner's `.local/a16/parity` directory.

Not performed: actual Safari/VoiceOver/NVDA, human participant or audio-backed AT sessions, physical-phone/touch, native macOS execution, complete zoom/short-height/real-font acceptance, independent replication of all A21 broad suites, hosted-check completion, deployment, or live-byte verification. A21's reported broad responsive/accessibility/CSP results remain integrator evidence. Actual linguistic/human boundaries and regional draft status remain open. Any new candidate needs an exact rebind and affected retests; this decision is bound only to the SHA and manifest above.
