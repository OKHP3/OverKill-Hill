# T02 Closeout Evidence

Date: 2026-09-08

Scope:
- `scripts/check-i18n-release.py`
- `tests/test-i18n-release.py`

What changed:
- Added reviewed-target integrity checks that compare the current localized target bytes with the reviewed ledger entry.
- Preserved source freshness handling while adding a separate `target_changed` status for stale or in-sync locales whose reviewed target bytes changed.
- Hardened adoption validation so forged provenance, duplicate routes, and mismatched hashes fail closed before any ledger write.
- Kept adoption narrow: only the explicitly reviewed locale-route pair is merged back into the shared state file.

Verification:
- `python3 -m unittest tests.test-i18n-release`

Observed result:
- 18 tests passed.
- Reviewed target drift now blocks the current blocking locale and remains advisory for non-blocking locales.
- Clean adoption still updates only the reviewed locale entry and leaves the provenance file unchanged.
