# A20 independent Option B acceptance

**ACCEPT WITH LIMITS for `b92c2a0673c66eac5598551e28cc6eb1961c5785` (PR81).** The requested Option B presentation passes this bounded independent review. This is not publication authorization, hosted acceptance, human/native-language approval, or full A20 completion.

## Exact release and preservation

Independent source review and browser execution used frozen candidate `5fde69f779326b0b2fee80e01b978d23ba35d79a` against main `98e512d26d9f60a7f5a07a11404a5829e22fd317`, in A20's isolated worktree. The supplied package was copied to an ignored A20 directory and served on the owned loopback port 58426. Each browser sample checked its manifest SHA identity. No candidate source files were edited.

The first manifest SHA-256 is `8fbfac9efbb8c3103aebbfa3b68ce78cc60fda9cd9cc7d67c230f8747e82252d`. Independent checks verify all 371 payload lengths and digests: 195 exact Git-byte matches and 176 CRLF-only differences, with zero other differences. Inventory: 56 HTML pages, 372 total files. See [independent integrity and preservation](../audit/a20-option-b-independent-2026-09-08.json).

The newer candidate's manifest SHA-256 is `97f69541b445bd7f1265a6cf1fc3665c949d384210c88014e53042a837e41518`. All 371 payloads are byte-identical to the independently tested first package. The 14-path Git delta contains only previous acceptance/deployment evidence and one test file; none is served. The supplied `release-current-verified` package passes the repository's exact-SHA verification. No reliance was placed on the integrator's rejected `release-current` directory. [Independent rebind](../audit/a20-option-b-rebind-2026-09-08.json) supports carrying forward the unchanged-payload browser results.

Across nine English, French, en-GB and es-MX homepage/project surfaces, all prior URL destinations, picture attributes, full status text and non-link paragraph text remain present. Registry bytes are unchanged. No image assets changed. The MurderBird image loads correctly and the motto remains `Precision · Protocol · Promptcraft`. All 47 remaining shipped HTML main elements compare identically with baseline, preserving archive, writing, manifesto, contact and unrelated content. This checks the current accepted baseline, not the superseded fallback-only historical whole-homepage equality premise.

## Independent gates and visitor behavior

- All 16 recorded structural, generated HTML/search/universe, cache, audit, link, locale, registry, disclosure, CSP and asset-weight gates passed. This includes six disclosure tests and seven registry tests. [Commands and output](../audit/a20-option-b-gates-2026-09-08.json).
- Focused accessibility QA passed four representative pages and all 56 shipped routes against A20's package server. Shared CSS contrast checks passed all 32 configured token pairs. These are bounded automated gates, not complete WCAG or spoken AT certification.
- On the newer candidate, all 13 live-edge unit tests passed, including negative fingerprint rejection. The old candidate's prior 12-test suite reproduced the stale-fingerprint fixture failure before switching to the repaired SHA; it is not concealed or attributed to production behavior.
- A20 authored and executed 54 Chromium/Firefox route samples: nine routes at 390 and 1280 CSS pixels with JavaScript, plus nine phone samples without JavaScript per browser. Every page returned 200, matched the served manifest, and had no horizontal overflow or page JavaScript error. All tested disclosure summaries opened with Enter and closed with Space. Actual Tab traversal reached the homepage disclosures; direct focus was used to exercise every individual summary. Native context and nested disclosures remain functional without page JavaScript. [Raw browser evidence](../audit/a20-option-b-browser-2026-09-08.json).
- The initial browser assertion incorrectly rejected any status brief inside any details element, including the intentionally collapsed orientation block. This produced 24 raw assertion failures; those records remain unchanged. Separate DOM checks across nine surfaces and eight fresh browser checks establish that every brief is outside its own Status and source details, and active-card briefs are visible. One archived orientation brief per homepage is appropriately inside More from the forge. [Explicit adjudication and retest](../audit/a20-option-b-brief-adjudication-2026-09-08.json). No other browser assertion failed. The first unbounded probe was stopped after stalling; only the completed bounded run is used as evidence.
- Selected work precedes Featured from the Forge, with supporting orientation below. Card actions remain outside status details. Full canonical summaries, source URLs and review dates are retained. Visible maturity/unknown-operation labels remain, including the material workbench RAG limit. Native summary controls have keyboard focus styling and usable disclosure behavior.
- Prototype paths are absent from the verified manifest. Requests to `/proposals/`, `/.local/a21-b/` and `/site-src/pages/index.main.html` return 404 from the isolated package server. This is package-boundary evidence, not a hosted-server policy claim.

Independent screenshots were visually inspected: [phone hero](../audit/screenshots/a20-option-b/home-390.png), [desktop hero](../audit/screenshots/a20-option-b/home-1280.png), [phone cards](../audit/screenshots/a20-option-b/cards-390.png), and [expanded context](../audit/screenshots/a20-option-b/context-390.png). The current artwork, dimensional cards, orange rules and retained prose are visible; no clipping or overlap was observed in these samples. Phone Selected work follows below the hero; this does not assert that all priority content is above the fold.

## Boundaries and next action

French freshness passes against its recorded AI review and source freeze. Existing condensed coverage remains; human/native approval is false. Regional en-GB/es-MX remain noindex, absent from public alternates/search, and unaccepted. German/Spanish stale draft records remain advisory. Structural parity does not certify translation quality. A15's separate copy proposal is not adopted.

Actual Safari/VoiceOver/NVDA, native macOS, physical phone/touch, real browser zoom, human font/visual judgment, and human/native-language acceptance remain unperformed. Fresh field-performance trials and full hosted/deployment acceptance are not claimed. The previous WebKit ordinary-link Tab environment limitation is not converted to a pass. The asset-weight check is not a latency benchmark.

A21 retains integration, required hosted gates, deployment and live acceptance. Any subsequent served-byte change requires affected retests and a new binding. This evidence-only commit adds review records and screenshots; it does not modify the candidate or trigger CI/deployment.
