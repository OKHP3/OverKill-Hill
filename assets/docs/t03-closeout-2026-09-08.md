# T03 Closeout

## Scope

- Fixed translation skill test discovery without renaming the existing hyphenated package test files.
- Added a focused CI workflow for the translation skill suites.
- Added a guard test that proves missing, empty, reduced, and failing suites are caught.

## What changed

- `scripts/test-translation-skills.py` now loads the five translation package tests and the i18n page-sync suite directly from file paths.
- `tests/test-translation-skill-discovery.py` exercises the runner against disposable fixtures.
- `.github/workflows/translation-skill-tests.yml` runs the guard test and the aggregate suite on changes to the translation packages.

## Verification

- `python3 tests/test-translation-skill-discovery.py`
- `python3 scripts/test-translation-skills.py`

Result: 59 translation and detector tests passed in the aggregate run.

## Notes

- The existing hyphenated test filenames were preserved.
- Standard `unittest discover` is still not a fit for those filenames, so the runner uses direct file loading instead.
