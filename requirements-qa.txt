# Pinned Python dependencies for the static-site QA and build helpers.
# Beautiful Soup supports structural checks; Pillow supports accepted still
# generation and byte/dimension validation. The site has no Python runtime dependency.
# Versions reflect the compatible local runtime
# inventory on 2026-09-04 (Python 3.14) and support the CI Python 3.11 job.
beautifulsoup4==4.15.0
soupsieve==2.9.2
typing-extensions==4.16.0
Pillow==12.3.0
