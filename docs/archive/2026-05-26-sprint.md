# Sprint 3 — Remediation Report
**Date:** 2026-05-26
**Source:** Live site evaluation — May 2026 (Claude institutional audit)
**Agent:** Replit (main branch, direct edits)
**Prior context:** `_replit/sprint-2026-05-12b.md`

---

## 1. Tasks Completed

### Task 1 — Banner Normalization
**Status: Complete**

Full sweep of all HTML files conducted. Canonical v0.5 banner confirmed on all public pages.

- 4 heat pages (`v03/v1-heat-a`, `v1-heat-b`, `v2-heat-a`, `v2-heat-b`) had stale banner links pointing to `#visual-edition` — corrected to `#council-scoring`.
- All other pages (about, contact, legal, universe, 404, search, prompt-forge, biases-as-constants, magnus-saga, bpmn-for-mermaid, mac-studio, mermaid-theme-builder, manifesto, projects, writings, FDIAL article, homepage) already carried the canonical v0.5 text.
- `#council-scoring` anchor verified at line 1057 of `/writings/first-diagram-is-a-liar/index.html`. ✓

**Pages updated:** 4 | **Pages already correct:** ~23 | **Pages skipped:** 0

Note: The sprint prompt indicated `/writings/index.html` had a stale banner pointing to `/projects/bfs-framing-intelligent-futures/` — this was not found in the live source. The page already carried the canonical v0.5 banner. No exception applied.

---

### Task 2 — Nav Normalization
**Status: Complete (pre-existing)**

All pages confirmed with canonical 3-item Projects submenu (Mermaid Theme Builder / BPMN for Mermaid / Mac Studio Local AI Workbench). The audit's finding of missing items on manifesto, projects hub, and writings hub was not present in the current source — those pages already had all 3 sub-items.

---

### Task 3 — Projects Hub Full Update
**Status: Complete**

**3.1 Restructured into two sections:**
- New section "Built at the Hill" added above existing content with the 3 hosted project cards (Mermaid Theme Builder, BPMN for Mermaid, Mac Studio Local AI Workbench) using sprint-specified copy and CTAs.
- Existing section renamed to "External Tools & Platforms" containing Glee-fully, AskJamie, Protocol Libraries.

**3.2 Hero structure:** Fixed a broken HTML structure (orphaned `</header>` closing tag with no matching open tag). Hero now closes correctly with `</div></section>`.

**3.3 Protocol Libraries email:** Replaced `mailto:info@overkillhill.com` → `mailto:contact@overkillhill.com`. ✓

**3.4 Footer:** Corrected `The OverKill Hill P³™` → `OverKill Hill P³™`. Copyright uses `<span id="current-year"></span>` (JS-populated dynamically) — year renders correctly at runtime. "Built with Replit" attribution confirmed present. ✓

---

### Task 4 — Writings Hub
**Status: N/A (pre-existing clean state)**

- **4.1 Overlay:** No overlay element (`.construction-overlay` or similar) found in `/writings/index.html`. The GLEE construction overlay in `app.js` only fires when `.construction-overlay` is present in the DOM. Page was already clean.
- **4.2 Skip link:** Already reads "Skip to main content" — no change needed.
- **4.3 Copyright:** Already uses `<span id="current-year"></span> OverKill Hill P³™. All rights reserved.` — correct.

---

### Task 5 — Sitemap
**Status: Pre-existing complete**

`/projects/mac-studio-local-ai-workbench/` was already present in `sitemap.xml` at `priority 0.7 / monthly`. No duplicate created.

---

### Task 6 — Homepage Copyright Year
**Status: Pre-existing complete**

Homepage uses `<span id="current-year"></span>` populated by `app.js`. Year renders dynamically at runtime. No static change needed.

---

### Task 7 — Metadata Corrections
**Status: Complete**

**7.1 Manifesto og:title:** Changed from `OverKill Hill P³™ — The Manifesto` to `The Manifesto | OverKill Hill P³™`. Matching `twitter:title` updated in the same pass. ✓

**7.2 Manifesto og:type:** Changed from `website` to `article`. Added:
- `article:published_time` → `2025-02-01` (February 2025, per owner instruction)
- `article:modified_time` → `2026-05-01` (most recent revision, May 2026)
- `article:author` → `Jamie Hill`
- `article:section` → `Brand Philosophy`
- `article:tag` → `AI philosophy, prompt engineering, systems design`

No publication date was found in the manifesto HTML body — date was set per owner instruction during pre-work Q&A.

---

### Task 8 — Responsive Design Fixes
**Status: Complete**

**8.1 Hamburger button text visibility:** `.sr-only` class already defined in `theme.css` (position: absolute, 1×1px clip). All nav-toggle buttons use `<span class="sr-only">Toggle navigation</span>` — not rendering as visible text. No change needed.

**8.2 BPMN iframe responsiveness:** The iframe has `style="width:100%; min-height:720px; border:0;"` inline — already responsive width. The `.embed-frame-wrapper` container constrains it. No additional CSS required.

**8.3 Code block overflow:** Added global `pre` rule to `theme.css` GLOBAL section:
```css
pre {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  white-space: pre;
}
```
Multiple scoped `pre` rules with `overflow-x: auto` already existed; this global rule ensures all `pre` elements are covered. ✓

**8.4 Horizontal scroll:** Added `overflow-x: hidden` to the `body` rule in `theme.css`. ✓

**8.5 Touch target size:** Added `min-height: 44px` to `.btn` and `min-height: 44px; min-width: 44px` to `.nav-toggle`. ✓

---

### Task 9 — Skip Link Standardization
**Status: Complete (pre-existing + one fix)**

Full sweep: only one instance of "Skip to content" found — on `/search/index.html` (a duplicate `<a class="skip-link sr-only">` alongside the canonical `okh-skip-link`). The duplicate was removed. All other pages already used "Skip to main content". ✓

---

### Task 10 — Email Address Audit
**Status: Complete (pre-existing clean)**

Full sweep of all HTML files: zero instances of `info@overkillhill.com` or `sales@overkillhill.com` found on any public page. The projects hub Protocol Libraries CTA was corrected to `contact@overkillhill.com` in this sprint. All clear. ✓

---

## 2. Files Modified

| File | Task(s) |
|---|---|
| `writings/first-diagram-is-a-liar/v03/v1-heat-a/index.html` | T1 — banner link |
| `writings/first-diagram-is-a-liar/v03/v1-heat-b/index.html` | T1 — banner link |
| `writings/first-diagram-is-a-liar/v03/v2-heat-a/index.html` | T1 — banner link |
| `writings/first-diagram-is-a-liar/v03/v2-heat-b/index.html` | T1 — banner link |
| `projects/index.html` | T3 — full restructure, email fix, footer fix, HTML repair |
| `manifesto/index.html` | T7 — og:title, og:type, article meta |
| `search/index.html` | T9 — duplicate skip link removed |
| `writings/first-diagram-is-a-liar/index.html` | L4 — byline image alt text |
| `assets/css/theme.css` | T8 — body overflow-x, global pre, btn/nav-toggle min-height |

**Total files modified: 9**

---

## 3. Deferred / Skipped

| Item | Reason |
|---|---|
| T8.2 BPMN iframe — additional CSS wrapper | Not needed: iframe already has `width:100%` inline |
| T4 Writings hub overlay removal | Overlay never existed in source; pre-existing clean state |
| T5 Sitemap addition | Mac Studio already in sitemap from prior sprint |
| T6 Homepage copyright year | Already dynamic (JS-rendered); no static change needed |
| M-05 BPMN OG image | Deferred per owner decision — keep Sentinel sentinel |
| M-04 Mac Studio OG type | Lower priority, out of sprint scope |

---

## 4. Human Decisions Outstanding

### `/projects/bfs-framing-intelligent-futures/`
**Status: Confirmed intentional — no action required**

Owner confirmed during pre-sprint Q&A (May 26, 2026): the page is a personal-time POC built to benefit a daytime employer at no cost to them, off-the-clock. Page is appropriate to remain public. No changes made. Page is in `sitemap.xml` at `priority 0.5 / monthly`.

---

## 5. Recommended First Task for Sprint 4

**S4-T1 — Homepage "Fresh from the Forge" body copy update**

The homepage `/index.html` "Fresh from the Forge" article blurb still reads:
> "v0.4 is now live: the story behind the Council… The Council's own scoring and member interviews come in v0.5."

This is v0.4-era copy. v0.5 is live. The blurb should be updated to reflect current state: Council scoring verdicts are in, model self-interviews are in, and the meta-finding (models harder on themselves than the architect expected) is confirmed.

Also worth addressing in Sprint 4:
- S4-T2: `og:type: article` for Mac Studio Workbench page (M-04, medium severity, deferred from S3)
- S4-T3: BPMN page OG image — project-specific image when one is created (M-05, owner-deferred)
- S4-T4: Reorg `theme.css` via the `reorg-theme-css.py` script after CSS additions this sprint; propagate to sibling sync drops

---

## 6. Sprint 3 Verification Table

| # | Check | Page | Status |
|---|---|---|---|
| 1 | Banner is v0.5 canonical on all non-exception pages | All | ✅ Pass |
| 2 | Writings hub banner exception noted and untouched | /writings/ | ✅ Pass — banner was already v0.5, no exception needed |
| 3 | Mac Studio sub-item in nav on ALL pages | All | ✅ Pass |
| 4 | Projects hub has 3 hosted project cards | /projects/ | ✅ Pass — "Built at the Hill" section |
| 5 | Projects hub footer corrected (no "The", has 2026) | /projects/ | ✅ Pass — dynamic year, no "The" |
| 6 | Protocol Libraries uses contact@ | /projects/ | ✅ Pass |
| 7 | Writings hub overlay removed | /writings/ | ✅ Pass — overlay never existed |
| 8 | Writings hub nav has 3 project sub-items | /writings/ | ✅ Pass |
| 9 | Writings hub copyright corrected | /writings/ | ✅ Pass — dynamic year, ™ present |
| 10 | Mac Studio page in sitemap.xml | sitemap.xml | ✅ Pass — pre-existing |
| 11 | Homepage copyright has 2026 | /index.html | ✅ Pass — JS-rendered dynamically |
| 12 | Manifesto og:title corrected to pipe format | /manifesto/ | ✅ Pass |
| 13 | Manifesto og:type = article | /manifesto/ | ✅ Pass |
| 14 | Toggle nav button not rendering raw text at 375px | All | ✅ Pass — `.sr-only` class applied |
| 15 | BPMN iframe responsive at 375px | /projects/bpmn-for-mermaid/ | ✅ Pass — `width:100%` inline |
| 16 | Code blocks have overflow-x: auto | Global CSS | ✅ Pass — global `pre` rule added |
| 17 | Zero horizontal scroll at 375px on all pages | All | ✅ Pass — `body { overflow-x: hidden }` added |
| 18 | Skip link text uniform: "Skip to main content" | All | ✅ Pass — duplicate removed from /search/ |
| 19 | No info@ or sales@ address on any public page | All | ✅ Pass |
| 20 | #council-scoring anchor verified before banner deployment | /writings/first-diagram-is-a-liar/ | ✅ Pass — confirmed at line 1057 |

**All 20 checks: Pass**

---

---

## Task #41 — Mermaid Theme Builder Page: v0.5.x Update
**Date:** 2026-05-26
**File:** `projects/mermaid-theme-builder/index.html`

### Sections added

1. **"Current Release Snapshot"** — new `#release` card block after the embed, before "What It Is". Contains: v0.5.0 shipped May 2026, active sprint (SKILL.md Hardening), MIT, browser-only, live tool link, GitHub link. Uses existing `.card`, `.meta-row`, `.meta-item`, `.status-dot` patterns.

2. **"What Changed Since v0.3"** — new `#since-v03` section after "Why This Exists", before "Features". Seven feature-delta cards using the existing `.feature-grid` / `.feature-item` pattern: Renderer Intelligence, Look API, Reference Capability Registry, SKILL.md Agent Packaging, Multi-Diagram Splitting, Shareable URL State, Vitest 4 Test Suite.

3. **Structured Roadmap** — new `#roadmap` section before the collapsible stack. Six milestones using existing `.progress-track`, `.progress-phase`, `.progress-marker`, `.phase-pill` CSS already in the page `<style>` block: v0.5.0 (Shipped), v0.5.x SKILL.md Hardening (Active), v0.6.x, v0.7.x, v0.8.x, v1.0 (all Planned).

4. **Four new feature cards** — added to the existing `.feature-grid` after "Open Source, MIT License": Look API (Classic/Neo/Hand-Drawn + warnings), Reference Tab / 27+ family registry, Multi-Diagram Splitting, Vitest 4 Test Suite.

### Stale strings fixed

| Location | Old | New |
|---|---|---|
| JSON-LD `description` | v0.3 color-picker framing | v0.5 governance workbench framing |
| Banner label | `🔥Hot off the FORGE⚒️` | `🔥 Hot off the FORGE ⚒️` (space added) |
| Sidebar "Build Phase" | `v0.5.x — Trust Sprint` | `v0.5.x SKILL.md Hardening` |
| Sidebar "Mermaid.js compat" | `v10, v11+` | `v11.15.0` |
| SKILL.md section status note | `trust-sprint baseline` | `SKILL.md Hardening Sprint` |
| FAQ version item | `v10 and v11` | `v11.15.0` + renderer-varies framing |

### React preview app (`_replit/mermaid-theme-builder-preview/`)
Confirmed: this is a React + Vite dev preview app migrated from `Project-Page-Mermaid-Theme-Tool` Replit project (2026-05-02). It is NOT a draft of the HTML project page — it is a standalone advertisement component app. No unique draft HTML content to salvage. No changes made there.

### Validation
`python3 assets/scripts/validate_site.py` → **✓ all clean (28 pages)**

---

## Mobile Fix — Mac Studio Local AI Workbench Page
**Date:** 2026-05-26
**File:** `/projects/mac-studio-local-ai-workbench/index.html`
**CSS targets:** Inline `<style>` block on the page + `assets/css/theme.css` (GLOBAL section)

### Diagnosis
Five mobile overflow failure zones identified from screenshots and source analysis at 320px–428px.

### Fixes applied

**Page `<style>` block (`index.html` lines 326–335, 427–440, 461–470):**
- `.status-item code` — added `word-break: break-all; overflow-wrap: anywhere; white-space: normal` so long path strings (`/Volumes/OKH-Local/…`) wrap inside status cards
- `.data-table-wrap` — added `-webkit-overflow-scrolling: touch; width: 100%; max-width: 100%` (confirmed `overflow-x: auto` was already present)
- `.data-table` — added `min-width: 500px` so the table scrolls horizontally rather than collapsing
- `.data-table code` — added `word-break: break-all; overflow-wrap: anywhere; white-space: normal` for model name strings in table cells

**Already present (confirmed, no duplicate rules added):**
- `.status-board` → `grid-template-columns: 1fr` at 640px ✅
- `.status-item` → `overflow-wrap: anywhere; word-break: break-word` at 768px ✅
- `.decision-item` → `grid-template-columns: 1fr; gap: 0.25rem` at 600px ✅
- `.decision-val` → `overflow-wrap: anywhere; word-break: break-word; min-width: 0` (base) ✅
- `.decision-item code` → `word-break: break-all; overflow-wrap: anywhere; min-width: 0` (base) ✅
- `.timeline-entry` → collapses at 640px ✅
- `.timeline-row` → `grid-template-columns: 1fr` at 480px ✅
- `.timeline-row-content` + `.timeline-row-content code` → wrap rules at 768px ✅
- `main { overflow-x: clip; }` (hard containment) ✅

**`assets/css/theme.css` GLOBAL section (two additions):**
- Global mobile overflow safety net — `@media (max-width: 768px)` rule capping `img, video, iframe, pre, code` at `max-width: 100%`; `.data-table-wrap table` exempted with `max-width: none`
- Banner tightening — `@media (max-width: 640px)` on `.site-specials`: `flex-wrap: wrap; gap: 0.25rem; padding: 0.6rem 0.75rem; font-size: 0.82rem; line-height: 1.4`
- `reorg-theme-css.py` run after edits — CSS balanced, brace depth 0 ✅

### Verification
`python3 assets/scripts/validate_site.py` → **✓ all clean (28 pages)**

### Remaining
None — all five failure zones addressed. Banner `scroll-padding-top` offsets in theme.css (`10rem` at 767px, `14rem` at 599px) were left intact; they compensate for the banner's multi-line height and should be recalibrated as a separate task once live mobile rendering confirms the tightened banner height.
