# W04 handoff: FoundRy recovery/export FAQ candidate

## Scope acknowledged

Branch: `coop/w04`  
Worktree: `.cache/coop-pertition/worktrees/w04`  
Exclusive outputs:

- `assets/docs/foundry-recovery-faq-2026-09-07.md`
- `docs/handoffs/coop-pertition-2026-09-07/w04.md`

No webpage, configuration, generated source, dependency, hosting, owner checkout, push, merge, deploy, or external message was changed.

## Result

The FAQ candidate is ready to apply to the existing FoundRy FAQ `<details>` block. It covers named projects, browser-origin storage, versioned backup/import, source ZIP versus Custom GPT exports, and the boundary between an export and a public capability release. The candidate preserves the existing “tools for making tools” origin story and maps each proposed block to current source evidence.

## Evidence and validation

- FoundRy page destination and origin story read from `projects/found-ry/index.html`.
- Capability workbench evidence read from `artifacts/mockup-sandbox/src/lib/capability-workbench.ts` and `artifacts/mockup-sandbox/src/pages/capability-workbench.tsx` in the owner checkout.
- Custom GPT workspace/export evidence read from `artifacts/mockup-sandbox/src/lib/creatorStorage.ts` and `artifacts/mockup-sandbox/src/pages/ExportPackage.tsx` in the owner checkout.
- Candidate checked for actual links/paths and for unsupported claims about servers, recovery, licensing, runtime behavior, behavioral validation, or publication authority.
- This handoff is a proposed, reviewable candidate. Applying it to `projects/found-ry/index.html` remains a separate integration decision.

## Commit

Pending local commit on `coop/w04`; no push or merge performed.
