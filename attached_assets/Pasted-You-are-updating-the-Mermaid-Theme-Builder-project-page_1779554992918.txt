You are updating the Mermaid Theme Builder project page on the OverKill Hill P³ website. The source file is the project page for the Mermaid Theme Builder — locate it by searching for the file that renders at /projects/mermaid-theme-builder/. It may be an HTML file, a Markdown file, or a Liquid/Jekyll template. Find the correct source file before making any changes.

The current page is materially out of date. It still describes the project as being in a V0.3.1-in-progress state with V0.4 and V0.5 as future planned sprints. The application shipped V0.5.0. The page must be rewritten to reflect reality.

Execute the following changes in order:

---

SECTION 1 — STATUS METADATA

Update all status indicators, badges, and metadata fields:
- Version badge: change from "V0.3 → V0.5 Active" to "V0.5.0 Shipped"
- Build phase: change from "V0.3.1 in progress" to "V0.5.0 — Current Release"
- Any "In Progress" arrows or incomplete markers referencing V0.3 through V0.5: mark as complete

---

SECTION 2 — FEATURES BLOCK

Replace the current features list with the following complete V0.5.0 feature set. Preserve the existing visual card/icon layout — only replace the content:

Feature 1 — Title: "Live Diagram Preview"
Content: Every color change reflects instantly across all diagram families. What you see is what renders.

Feature 2 — Title: "27+ Diagram Families"
Content: Flowchart, Sequence, Class, State, ER, Gantt, Pie, Git Graph, Mindmap, Timeline, Quadrant, C4, Architecture Beta, Kanban, BPMN, Sankey, XY Chart, ZenUML, and more. Each family gets family-specific theming overlays, not a generic CSS dump.

Feature 3 — Title: "Three Rendering Looks"
Content: Classic (universal), Neo (modern flat), and Hand-Drawn (Rough.js). The tool tells you which looks your target renderer actually supports before you paste and find out the hard way.

Feature 4 — Title: "7 Renderer Profiles"
Content: mermaid.live, GitHub, GitLab, Notion, Obsidian, Confluence, and CLI — each with a full compatibility matrix covering init directive support, themeVariables depth, classDef availability, CSS injection, and custom fonts.

Feature 5 — Title: "5-Tier Typography Hierarchy"
Content: Diagram Title → Subgraph → Nested Subgraph → Node Label → Edge Label. Enforced parent-child sizing constraints. Exported as part of the prompt scaffold so AI tools stay typographically consistent across regenerations.

Feature 6 — Title: "7 Built-In Palettes"
Content: Four utility palettes (Ocean Depth, Forest Sage, Slate Ember, Violet Mist) and three brand palettes (OverKill Hill P³, Glee-fully, AskJamie). All grounded in production design tokens.

Feature 7 — Title: "Five Export Formats"
Content: Styled Code (paste-ready), Markdown Bootstrap (documentation-ready), AI Prompt Scaffold (LLM pre-prompting with full typography and classDef contract), Markdown with attribution, and Extract + Re-theme (pull and replace an existing theme).

Feature 8 — Title: "Extract Mode"
Content: Paste existing themed Mermaid code. The tool identifies the current palette, strips the old init block, and lets you replace it with any of the seven palettes without touching your diagram content.

Feature 9 — Title: "Agent Skill (SKILL.md)"
Content: Ships a headless SKILL.md-compatible agent skill for Claude Code, GitHub Copilot, Cursor, and VS Code. The skill packages the full palette registry, renderer profiles, and prompt scaffold logic into a format AI coding agents can use without the UI.

Feature 10 — Title: "Browser-Only — No Server"
Content: Runs 100% client-side. No login, no account, no telemetry. Close the tab and nothing persists anywhere you didn't explicitly export it.

---

SECTION 3 — BUILD PROGRESS / ROADMAP BLOCK

Replace the existing build progress section entirely with the following. Preserve the visual timeline or checklist format that currently exists — replace only the content.

COMPLETED:

V0.1 — Foundation
Core paste → detect → palette → preview → export loop. Four utility palettes. Basic diagram family detection. Prompt scaffold modal. GitHub Pages deployment.

V0.2 — Brand System Integration
Three brand palettes added (OverKill Hill P³, Glee-fully, AskJamie) sourced from production design tokens. Renderer capability notes. Family-specific theming overlays (familyTheming.ts). Extract mode.

V0.3 — Architecture Hardening
React 19 + Vite 8 + TypeScript 5.9 monorepo structure. Full Compose tab: 5-tier typography configuration with enforced parent-child constraints. pnpm workspaces. OKH P³ Forge design language applied to the builder UI itself.

V0.4 — Renderer Intelligence
7 renderer profiles with full parity matrix. Mermaid v11.15.0 look API support: Classic, Neo, Hand-Drawn with per-renderer support warnings. Dark/light/system mode (themeMode.ts). Examples tab with 26+ authoring reference files. Diff view between original and themed code.

V0.5 — Governance Layer + Agent Skill
Reference tab: 27+ diagram family capability registry, full renderer parity matrix, class library. Five export formats. SKILL.md agent skill package for Claude Code, Copilot, Cursor, and VS Code. Vitest 4 test suite. Multi-diagram splitting. Shareable URL encoding for theme state.

CURRENT: V0.5.0 — Shipped May 2026

---

UPCOMING SPRINTS:

V0.5.x — SKILL.md Hardening Sprint (Near-term)
The SKILL.md agent skill package gets production-hardened: YAML frontmatter conformance for all agent platforms, look API documentation added to the skill workflow, all referenced asset and script files verified and populated, test suite execution confirmed. Version parity between skill metadata and package.json locked. Scope firewall reference document created.

V0.6 — Native Capability + Ko-fi Artifact Sprint (Q3 2026)
Capacitor.js wrapper enabling iOS and Android distribution. Native device capabilities: Files app integration for importing .mmd files, Share Sheet export, clipboard detection on launch. Ko-fi downloadable artifact line: branded PPTX templates, prompt scaffold packs, and the Mermaid Diagram Standard document. PWA manifest hardening for offline-capable web use.

V0.7 — Session Persistence + Multi-Diagram Canvas (Q4 2026)
Saved theme sessions: name, export, and reload complete theme configurations. Multi-diagram canvas: apply a single theme across a set of diagrams simultaneously and export as a cohesive package. Theme diff history: compare what changed between theme iterations. Mermaid Chart platform integration using the enterprise API tier.

V0.8 — Collaboration + Governance Hardening (Q1 2027)
Shareable theme packages: a single URL that encodes a complete theme definition including palette, typography contract, classDef library, and target renderer profile. Team import/export for shared governance standards. Diagram audit mode: scan a repository of .mmd files and flag style drift against a reference theme. Custom palette builder with WCAG contrast validation built in.

V1.0 — Production Release (H1 2027)
Full App Store release (iOS + iPadOS). Verified compatibility with Mermaid Chart enterprise tier. Complete documentation site. Community palette registry: submit and browse palettes from the Mermaid.js community. Stability commitment: no breaking changes to export formats or SKILL.md API. First stable release of the SKILL.md agent skill as a standalone installable package.

---

SECTION 4 — FAQ UPDATES

Locate the FAQ section and update the following specific entries:

Entry: "Why doesn't it support dark/light mode toggle in the builder itself?"
Replace the answer with: "Dark/light/system mode is live as of V0.4. Toggle it in the builder header. The preference is preserved in your URL state."

Entry referencing V0.4 roadmap items as future: update to reflect V0.4 shipped.

Entry referencing V0.5 trust sprint as future: update to reflect V0.5.0 shipped May 2026.

Add the following new FAQ entry if one does not already exist:
Question: "Is there a version I can use without opening a browser?"
Answer: "Yes. The SKILL.md agent skill packages the theming logic for Claude Code, GitHub Copilot, Cursor, and VS Code. Install it from the skills/ directory in the GitHub repository. It runs headlessly inside your AI coding agent — no browser required."

---

SECTION 5 — PROJECT INFO SIDEBAR

Update the metadata sidebar block:
- Status: V0.5.0 Shipped
- Build Phase: Active Development
- Mermaid.js compat: v10, v11+ (tested against v11.15.0)
- Keep all other fields as-is

---

SECTION 6 — DEVELOPMENT UPDATE CALLOUT

Locate the italic developer note/callout block that currently describes V0.3.1 as in-progress. Replace its content with:

"V0.5.0 shipped May 2026. The governance layer is complete: 27+ diagram families, 7 renderer profiles, 5-tier typography, five export formats, and a headless SKILL.md agent skill. The builder runs entirely in the browser with no backend, no login, and no data collection. The next sprint focuses on native device capabilities and Ko-fi artifact distribution."

Preserve whatever visual treatment (blockquote, callout box, etc.) currently wraps this content.

---

After making all changes, verify the page renders without broken links or missing asset references. Do not change the page's URL, navigation structure, visual design system, or brand voice. Preserve all existing SEO metadata tags and update only the content-facing values where version numbers are mentioned.
